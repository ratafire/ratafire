<html>
		<%= form_for current_user, :url => users_path, :html => { :multipart => true } do |form| %>
  		<%= form.file_field :profilelarge %>
  		<div class="btn btn-medium btn-medium-green">
			<%= form.submit "Upload", class: "btn-submit" %>
		</div>
		<% end %>
</html>