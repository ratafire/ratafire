<% if @reward.image.present? %>
	$('#add-reward-image').remove();
	$('#active-campaign-reward').prepend("<%= j render(:partial => 'profile/components/reward_image', :locals => {:reward => @reward}) %>");
<% end %>	