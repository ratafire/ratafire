<!-- Video-->
<% if @video != nil %>
	<%= render 'projects/project_videoplayer'%>
<% end %>

<!-- Artwork without video -->
<% if @artwork != nil && @video == nil %>
	<%= render 'shared/artworkdisplay'%>
<% end %>

<!-- Artwork with video -->
<% if @artwork != nil && @video != nil  %>
	<%= render 'shared/artwork_small_display'%>
<% end %>